import { st } from 'ui';

export const Layout = st('div', {
  background: '$background',
  position: 'relative',
  minHeight: '100vh',
  height: 'auto',
});

export const Container = st('main', {
  paddingTop: '20px',
  paddingBottom: '7rem',
});
