import { styled } from '@stitches/react';

export const Layout = styled('div', {
  background: '#F3FAF8',
  position: 'relative',
  height: 'auto',
});

export const Container = styled('main', {
  paddingTop: '20px',
  paddingBottom: '7rem',
});
